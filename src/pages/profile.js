import { setTitle } from "../utils";

const ProFile = {
   before_render() { setTitle("Profile Page") },
   after_render() { },
   render() {
      return /*html*/   `
        

      `

   }
}
export default ProFile